import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'movies_cesar-melia',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
